# Base + Hardhat + Solidity Starter

This is a minimal Hardhat project designed for deploying simple smart contracts to the Base blockchain.

## Commands

Install dependencies:
Compile:Deploy (Base Goerli):
